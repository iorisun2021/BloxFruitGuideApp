export default function Button() {
  return (
    <Button>
      style=
      {{
        marginTop: 40,
        justifyContent: "center",
        borderRadius: 8,
        backgroundColor: "#4083C7",
        width: 160,
        height: 40,
      }}
    </Button>
  );
}
